// Vite build entry: imports the global SCSS so Vite emits the compiled CSS bundle.
// This file itself produces no meaningful JS output; see vite.config.js for how the
// emitted JS chunk is discarded and only the CSS asset (wwwroot/css/app.css) is kept.
import './app.scss';
